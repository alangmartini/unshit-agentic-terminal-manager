### Added

- **Readable Markdown editing and dedicated file tabs.** Markdown editors now
  open with a rendered side preview that follows buffer edits, while explorer
  hover feedback uses an immediate, unambiguous row highlight. Open file
  editors get their own horizontal tab strip instead of sharing the terminal
  strip.
